
#include <stdio.h>
#include <stdlib.h>
#include "BTT_Alih.h"
int main() {
  Lista *lista

      lista_inicializar(&lista);
  lista_inserir(&lista, 'A');
  lista_inserir(&lista, 'B');
  lista_inserir(&lista, 'C');
  lista_inserir(&lista, 'D');
  lista_inserir(&lista, 'E');
  lista_inserir(&lista, 'F');
}

return 0;
}